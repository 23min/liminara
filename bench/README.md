# dag-map bench

Layout evolution harness for `dag-map`. Gitignored. Never published. Internal developer tool.

See `work/epics/E-DAGBENCH-layout-evolution/` in the Liminara repo for the spec.

## Run

    cd dag-map/bench
    npm test
